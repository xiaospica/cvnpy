# Python
*.pyc
*.pyo
*.pyd
*.egg-info/

# Jupyter
.ipynb_checkpoints

# IDE
.vscode
.idea
*.wpr
*.wpu
.vs
x64

# Temp
build
dist
*.local

# VeighNa
.vntrader

# Config files (ignore actual config, keep examples)
config/myapp.ini
config/*.ini
!config/*.ini.example
!config/myapp.ini.example

# Visual Studio intermediate files
*.exp
*.iobj
*.ipdb
*.pdb

# Documents
_build
_static
_templates

# Misc
.DS_Store
*.mo

# Alpha
lab/
/.mypy_cache

# Local secrets (do not commit)
vnpy_signal_strategy/mysql_signal_setting.json

# P0-1 凭证 / P0-2 路径外置 (.env + yaml configs)
# .env / .env.production / .env.staging / .env.local 等都忽略
# 唯独 .env.example 必须提交 (给新部署者参考)
.env
.env.*
!.env.example

# config/strategies.production.yaml / .staging.yaml / .local.yaml 等忽略
# 唯独 config/strategies.example.yaml 必须提交
config/*.production.yaml
config/*.staging.yaml
config/*.local.yaml
config/*.production.yml
config/*.staging.yml
config/*.local.yml

stock_data
api.json
vnpy_tushare
.trae
vnpy_ctabacktester
vnpy_ctastrategy
vnpy_signal_strategy
vnpy_xt-main
vnpy_webtrader-main
